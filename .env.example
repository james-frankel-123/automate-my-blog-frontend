# AutoBlog Frontend Environment Variables

# Backend API URL (replace with your actual backend URL)
REACT_APP_API_URL=https://your-backend-url.vercel.app

# Development API URL (for local development)
# REACT_APP_API_URL=http://localhost:3001

# Stripe Publishable Key (for embedded checkout)
# Get this from Stripe Dashboard -> Developers -> API Keys
# Use test key (pk_test_...) for development, live key (pk_live_...) for production
REACT_APP_STRIPE_PUBLIC_KEY=pk_test_your_publishable_key_here

# Feature flag: streaming (SSE) for job progress. Set to false to disable streaming
# and use polling only. Useful for gradual rollout or debugging.
# REACT_APP_STREAMING_ENABLED=false

# GitHub repo for dev-only rebase reminder and staging promote bar (org/repo). Defaults to Automate-My-Blog/automate-my-blog-frontend
# REACT_APP_GITHUB_REPO=Automate-My-Blog/automate-my-blog-frontend

# Set to true on the staging Vercel project so the "Promote to production" footer bar is shown
# REACT_APP_STAGING=true

# Production E2E full-flow test (signup + content generation). Optional; defaults to e2e-production-demo-{timestamp}@e2etest.com
# E2E_PRODUCTION_TEST_EMAIL=e2e-production-demo@yourdomain.com
# E2E_PRODUCTION_TEST_PASSWORD=YourSecurePassword123!